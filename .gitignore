# Never commit the scaffold/authoring folder itself — only project/,
# .agents/, and the root *.md files are meant to ship. See AGENTS.md's
# "Git workflow — always follow" section, rule 2.
scaffold/
